<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Kalo kamu list semua app, Android 11+ butuh ini -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Media permission (opsional sesuai fitur kamu) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StarLauncher"
        tools:targetApi="31">

        <!-- 1) HOME activity (launcher sesungguhnya) -->
        <!-- NOTE:
             - TANPA category LAUNCHER
             - optional: taskAffinity kosong + singleTask biar nempel sebagai home task
        -->
        <activity
            android:name="com.lyciv.star.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.StarLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 2) Activity untuk icon di app drawer (Settings/About, dsb) -->
        <!-- Biar user bisa buka app dari drawer tanpa bentrok sama HOME -->
        <activity
            android:name="com.lyciv.star.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.StarLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
